# `explain_page_classification`

Giải thích `NAVIGATION` và `RESOURCE` độc lập với MIME thực tế.

Input:

```json
{"page_id": "string"}
```

Flag trường hợp resource URL trả `text/html`, redirect sang HTML hoặc navigation trả binary.

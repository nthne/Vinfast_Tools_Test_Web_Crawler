# `inspect_source`

Đọc cấu hình một source crawler mà không thay đổi dữ liệu.

Input:

```json
{"source_id": "string"}
```

Đọc source detail, crawl config, rules, domain, enabled state và degraded state.

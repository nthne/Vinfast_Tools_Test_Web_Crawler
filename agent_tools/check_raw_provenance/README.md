# `check_raw_provenance`

Đối chiếu page view markdown với RAW clean preview và provenance của export.

Input:

```json
{"page_id": "string"}
```

Tool chỉ trả metrics/evidence, không trả raw body hoặc signed URL.

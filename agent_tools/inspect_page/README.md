# `inspect_page`

Đọc metadata của một page và trả về summary an toàn, không trả toàn bộ content.

Input:

```json
{"page_id": "string"}
```

Bao gồm URL, kind, MIME, status, route, độ dài content, title, depth và warning.

# `explain_clean_output_length`

Giải thích vì sao clean output dài hơn/ngắn hơn input bằng retention ratio và trạng thái degraded/reverted.

Input:

```json
{"page_id": "string", "mode": "SAFE"}
```

Không suy luận từ độ dài riêng lẻ; tool trả về số đo preview làm evidence.
